/**
 * Self-hosted font files live in public/fonts/.
 * CSS: src/styles/fonts.css
 *
 * To refresh from Google (Chrome UA), download the latin CSS, then copy the
 * latin + latin-ext woff2 URLs into public/fonts as:
 *   inter-latin.woff2, inter-latin-ext.woff2,
 *   oswald-latin.woff2, oswald-latin-ext.woff2
 *
 * Inter/Oswald Google CSS currently serves variable files (one file covers weights).
 */
console.log("Fonts are vendored under public/fonts/. See src/styles/fonts.css.");
