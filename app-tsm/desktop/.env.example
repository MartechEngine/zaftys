# URL opened by the Tauri WebView (hosted TSM — never embed service_role here)
# Local smoke:
TSM_DESKTOP_URL=http://localhost:3000
# Staging / prod (when hosted):
# TSM_DESKTOP_URL=https://tsm-staging.example.com
#
# Ops: append Google redirect on Supabase (do NOT rotate the Web client):
#   {TSM_PUBLIC_URL}/api/auth/tranzfort/callback
