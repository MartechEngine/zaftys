# Gated PDFs — HTTP blocked; PHP streams via /api/report-pdf.php
<IfModule mod_authz_core.c>
  <FilesMatch "\.(?i:pdf)$">
    Require all denied
  </FilesMatch>
</IfModule>
<IfModule !mod_authz_core.c>
  <FilesMatch "\.(?i:pdf)$">
    Deny from all
  </FilesMatch>
</IfModule>
